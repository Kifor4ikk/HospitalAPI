server.port: 1488
server.address: localhost
---
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:1489/PatientBase
    username: postgres
    password: pass